[package]
name = "rx-async"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
env_logger = "0.11"
libbladerf-rs = { path = "../..", features = ["tokio"] }
log = "0.4"
tokio = { version = "1", features = ["rt", "macros", "time"] }
